<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ui"
    android:versionCode="1"
    android:versionName="1.0" >
    <uses-permission android:name="android.permission.INTERNET"/>

    <uses-sdk
        android:minSdkVersion="8"
        android:targetSdkVersion="21" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/app_icon"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name="electricity.MainActivity"
            android:label="@string/app_name"
            android:theme="@android:style/Theme.NoTitleBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="electricity.UI_menu"
            android:theme="@android:style/Theme.NoTitleBar"></activity>
        <activity android:name="electricity.Get_Statistics"></activity>
        <activity android:name="electricity.Cal_money"></activity>    
        <activity android:name="electricity.chk_onoff_main"></activity>    
        <activity android:name="electricity.show_graph_group"></activity>
        <activity android:name="electricity.pi_graph"></activity>
        <activity android:name="electricity.show_graph"></activity>
    </application>

</manifest>
